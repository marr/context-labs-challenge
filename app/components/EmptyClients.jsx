import { useSearchParams } from "@remix-run/react";

export default function EmptyClients() {
    return (
        <main className="empty-clients">
            Sorry, no clients were found.
        </main>
    )
}